services:
  - type: web
    name: kick-stake-drops-bot
    runtime: python
    plan: free
    healthCheckPath: /health
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: DASH_USER
        value: "admin"
